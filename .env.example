# ===== Server Environment (copy to backend/.env) =====
JWT_SECRET=change_this_to_a_long_random_secret_minimum_32_chars
ALLOWED_ORIGINS=app://.

# ===== SMS Provider (optional) =====
SMS_PROVIDER=mock
SMS_API_KEY=
SMS_API_URL=
SMS_SENDER_ID=MSHALH

# ===== Docker Compose =====
# Used by docker-compose.yml only
